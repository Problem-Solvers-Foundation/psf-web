{
  "version": 2,
  "buildCommand": "cd docs-site && npm install && npm run build",
  "rewrites": [
    {
      "source": "/docs/:path*",
      "destination": "/docs-site/build/:path*"
    },
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
