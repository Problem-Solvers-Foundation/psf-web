<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Application Report - <%= application.fullName %></title>
  <style>
    @media print {
      body { margin: 0; }
      .no-print { display: none; }
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Arial', sans-serif;
      line-height: 1.6;
      color: #333;
      background: #fff;
      padding: 40px;
      max-width: 210mm;
      margin: 0 auto;
    }

    .header {
      border-bottom: 3px solid #117dd4;
      padding-bottom: 20px;
      margin-bottom: 30px;
    }

    .header h1 {
      color: #117dd4;
      font-size: 28px;
      margin-bottom: 5px;
    }

    .header .subtitle {
      color: #666;
      font-size: 14px;
    }

    .meta-info {
      background: #f5f5f5;
      padding: 15px;
      border-radius: 5px;
      margin-bottom: 30px;
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 10px;
    }

    .meta-item {
      font-size: 13px;
    }

    .meta-item strong {
      color: #117dd4;
    }

    .section {
      margin-bottom: 25px;
      page-break-inside: avoid;
    }

    .section-title {
      color: #117dd4;
      font-size: 18px;
      font-weight: bold;
      border-bottom: 2px solid #117dd4;
      padding-bottom: 5px;
      margin-bottom: 15px;
    }

    .field {
      margin-bottom: 15px;
    }

    .field-label {
      font-weight: bold;
      color: #555;
      font-size: 13px;
      margin-bottom: 5px;
    }

    .field-value {
      color: #333;
      font-size: 14px;
      padding-left: 10px;
      white-space: pre-wrap;
    }

    .skills {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
      margin-top: 5px;
    }

    .skill-badge {
      background: #e3f2fd;
      color: #1976d2;
      padding: 4px 12px;
      border-radius: 12px;
      font-size: 12px;
      font-weight: 500;
    }

    .status-badge {
      display: inline-block;
      padding: 6px 16px;
      border-radius: 20px;
      font-size: 13px;
      font-weight: bold;
      text-transform: uppercase;
    }

    .status-pending { background: #fff4e5; color: #ff9800; }
    .status-reviewing { background: #e3f2fd; color: #2196f3; }
    .status-approved { background: #e8f5e9; color: #4caf50; }
    .status-rejected { background: #ffebee; color: #f44336; }

    .review-section {
      background: #fafafa;
      padding: 20px;
      border-left: 4px solid #117dd4;
      margin-top: 30px;
      page-break-inside: avoid;
    }

    .print-button {
      position: fixed;
      top: 20px;
      right: 20px;
      background: #117dd4;
      color: white;
      border: none;
      padding: 12px 24px;
      border-radius: 5px;
      cursor: pointer;
      font-size: 14px;
      font-weight: bold;
      box-shadow: 0 2px 5px rgba(0,0,0,0.2);
    }

    .print-button:hover {
      background: #0d5fa3;
    }

    .footer {
      margin-top: 40px;
      padding-top: 20px;
      border-top: 1px solid #ddd;
      text-align: center;
      color: #999;
      font-size: 12px;
    }
  </style>
</head>
<body>
  <button class="print-button no-print" onclick="window.print()">🖨️ Print / Save as PDF</button>

  <!-- Header -->
  <div class="header">
    <h1>Problem Solvers Foundation</h1>
    <div class="subtitle">Application Report</div>
  </div>

  <!-- Meta Information -->
  <div class="meta-info">
    <div class="meta-item">
      <strong>Application ID:</strong> <%= application.id %>
    </div>
    <div class="meta-item">
      <strong>Submitted:</strong> <%= application.submittedAt ? new Date(application.submittedAt).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) : 'N/A' %>
    </div>
    <div class="meta-item">
      <strong>Status:</strong>
      <span class="status-badge status-<%= application.status || 'pending' %>">
        <%= application.status || 'pending' %>
      </span>
    </div>
    <div class="meta-item">
      <% if (application.reviewedAt) { %>
        <strong>Reviewed:</strong> <%= new Date(application.reviewedAt).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) %>
      <% } else { %>
        <strong>Reviewed:</strong> Not yet
      <% } %>
    </div>
  </div>

  <!-- Basic Information -->
  <div class="section">
    <div class="section-title">📋 Basic Information</div>

    <div class="field">
      <div class="field-label">Full Name</div>
      <div class="field-value"><%= application.fullName || 'N/A' %></div>
    </div>

    <div class="field">
      <div class="field-label">Email</div>
      <div class="field-value"><%= application.email || 'N/A' %></div>
    </div>

    <div class="field">
      <div class="field-label">Location</div>
      <div class="field-value"><%= application.location || 'N/A' %></div>
    </div>

    <div class="field">
      <div class="field-label">Age</div>
      <div class="field-value"><%= application.age || 'N/A' %></div>
    </div>

    <% if (application.linkedin) { %>
      <div class="field">
        <div class="field-label">LinkedIn</div>
        <div class="field-value"><%= application.linkedin %></div>
      </div>
    <% } %>

    <% if (application.instagram) { %>
      <div class="field">
        <div class="field-label">Instagram</div>
        <div class="field-value"><%= application.instagram %></div>
      </div>
    <% } %>
  </div>

  <!-- Motivation -->
  <div class="section">
    <div class="section-title">💡 Motivation and Purpose</div>
    <div class="field">
      <div class="field-label">Why do you want to join Problem Solvers?</div>
      <div class="field-value"><%= application.motivation || 'N/A' %></div>
    </div>
  </div>

  <!-- Projects -->
  <div class="section">
    <div class="section-title">🚀 Projects and Initiatives</div>
    <div class="field">
      <div class="field-label">Has existing project?</div>
      <div class="field-value"><%= application.hasProject || 'N/A' %></div>
    </div>

    <% if (application.projectDetails) { %>
      <div class="field">
        <div class="field-label">Project Details</div>
        <div class="field-value"><%= application.projectDetails %></div>
      </div>
    <% } %>
  </div>

  <!-- Areas of Interest -->
  <div class="section">
    <div class="section-title">🎯 Areas of Interest</div>
    <div class="field">
      <div class="field-label">Problems passionate about solving</div>
      <div class="field-value"><%= application.problemAreas || 'N/A' %></div>
    </div>
  </div>

  <!-- Skills -->
  <div class="section">
    <div class="section-title">🛠️ Skills</div>
    <div class="field">
      <div class="field-label">Selected Skills</div>
      <div class="skills">
        <% if (application.skills && Array.isArray(application.skills)) { %>
          <% application.skills.forEach(skill => { %>
            <span class="skill-badge"><%= skill %></span>
          <% }); %>
        <% } else { %>
          <span class="field-value">None selected</span>
        <% } %>
      </div>
    </div>

    <% if (application.skillsOther) { %>
      <div class="field">
        <div class="field-label">Other Skills</div>
        <div class="field-value"><%= application.skillsOther %></div>
      </div>
    <% } %>
  </div>

  <!-- Collaboration -->
  <div class="section">
    <div class="section-title">🤝 Collaboration</div>
    <div class="field">
      <div class="field-label">Interested in collaborating?</div>
      <div class="field-value"><%= application.collaboration || 'N/A' %></div>
    </div>
  </div>

  <!-- Additional Information -->
  <% if (application.additionalInfo) { %>
    <div class="section">
      <div class="section-title">📝 Additional Information</div>
      <div class="field-value"><%= application.additionalInfo %></div>
    </div>
  <% } %>

  <!-- Review Section -->
  <% if (application.reviewNotes || application.score || application.reviewedBy) { %>
    <div class="review-section">
      <div class="section-title">👤 Review Information</div>

      <% if (application.score) { %>
        <div class="field">
          <div class="field-label">Score</div>
          <div class="field-value"><%= application.score %> / 100</div>
        </div>
      <% } %>

      <% if (application.priority) { %>
        <div class="field">
          <div class="field-label">Priority</div>
          <div class="field-value"><%= application.priority %></div>
        </div>
      <% } %>

      <% if (application.interviewDate) { %>
        <div class="field">
          <div class="field-label">Interview Date</div>
          <div class="field-value"><%= new Date(application.interviewDate).toLocaleString() %></div>
        </div>
      <% } %>

      <% if (application.tags) { %>
        <div class="field">
          <div class="field-label">Tags</div>
          <div class="field-value"><%= application.tags %></div>
        </div>
      <% } %>

      <% if (application.reviewedBy) { %>
        <div class="field">
          <div class="field-label">Reviewed By</div>
          <div class="field-value"><%= application.reviewedBy %></div>
        </div>
      <% } %>

      <% if (application.reviewNotes) { %>
        <div class="field">
          <div class="field-label">Review Notes</div>
          <div class="field-value"><%= application.reviewNotes %></div>
        </div>
      <% } %>
    </div>
  <% } %>

  <!-- Footer -->
  <div class="footer">
    <p>Problem Solvers Foundation - Application Report</p>
    <p>Generated on <%= new Date().toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric', hour: '2-digit', minute: '2-digit' }) %></p>
  </div>

  <script>
    // Auto-print when opened (optional - can be removed if not desired)
    // window.onload = function() { window.print(); };
  </script>
</body>
</html>
